'use strict';

/**
 * assignment-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::assignment-submission.assignment-submission');
