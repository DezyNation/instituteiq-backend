'use strict';

/**
 * assignments-issued router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::assignments-issued.assignments-issued');
